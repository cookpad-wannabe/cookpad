<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body text-center">
      <p>welcome</p>
      <%- include ("./partials/post.ejs") %> <% result.map((item) => { %>

      <h3><%= item.name %></h3>
      <form action="recipes/<%=item.id%>/user_method=DELETE">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
      <a href="recipes/<%= item.id %>/delete?_method=DELETE"
        ><button type="button" class="btn btn-danger">Delete</button></a
      >

      <% }); %>
    </div>
  </div>
</div>
